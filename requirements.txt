# Rangos en vez de versiones clavadas: el esqueleto tiene que construir hoy
# aunque el indice de paquetes haya movido una version menor.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
pydantic-settings>=2.6,<3.0
httpx>=0.27,<1.0
pillow>=10.0,<12.0
numpy>=1.26,<3.0
pytesseract>=0.3.10,<1.0
# Subida de ficheros en el endpoint de verificacion.  FastAPI no lo trae:
# sin el, cualquier ruta que reciba un fichero revienta al importarse, no al
# llamarse, asi que la API entera deja de arrancar.
python-multipart>=0.0.9,<1.0

# Reconocimiento facial: deteccion (SCRFD) y embeddings (ArcFace), las dos
# dentro del paquete buffalo_l de InsightFace y corriendo sobre onnxruntime.
#
# Se usa SCRFD y no el detector YOLO propio del otro proyecto porque ese
# arrastra ultralytics + torch + torchvision, unos 2 GB de imagen, para una
# pieza que aqui no es la protagonista. El detector vive detras de una
# interfaz por si algun dia compensa cambiarlo.
insightface>=0.7,<2.0
onnxruntime>=1.18,<2.0
opencv-python-headless>=4.9,<6.0

# Pruebas
pytest>=8.3,<9.0
